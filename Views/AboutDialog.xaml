<Window x:Class="AdbPacketCapture.Views.AboutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AdbPacketCapture.Views"
        Title="ADB Packet Capture" Height="223" Width="308"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <StackPanel Margin="20">
        <TextBlock Text="ADB Packet Capture" 
                   FontSize="16" 
                   FontWeight="Bold"
                   HorizontalAlignment="Center"/>
        <TextBlock Text="Version: 1.00.000" 
                   Margin="0,10,0,0"
                   HorizontalAlignment="Center"/>
        <TextBlock Text="Developer: Acsis-yjm" 
                   Margin="0,5,0,0"
                   HorizontalAlignment="Center"/>
        <TextBlock Text="Copyright © 2024" 
                   Margin="0,5,0,0"
                   HorizontalAlignment="Center"/>
        <Button Content="OK" 
                Width="60" 
                Height="25"
                Margin="0,20,0,0"
                HorizontalAlignment="Center"
                Click="OkButton_Click"/>
    </StackPanel>
</Window>